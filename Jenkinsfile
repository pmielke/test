pipeline {
         agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'echo hi there'
                exit 1
            }
        }
    }
}
